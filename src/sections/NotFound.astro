<div
  class="flex flex-col max-w-sm mx-auto md:min-w-96 items-center justify-center min-h-screen p-4 sm:p-8 rounded-md shadow-md bg-secondary"
>
  <h1 class="text-4xl sm:text-6xl font-wide mb-4">404</h1>
  <p class="text-center text-lg sm:text-2xl mb-6">
    Lo sentimos, la página que buscas no se encontró.
  </p>
  <button
    id="button"
    type="button"
    class="w-100 sm:w-auto text-white bg-accent border border-gray-300 hover:bg-secondary font-regular rounded-lg text-sm px-5 py-2.5 cursor-pointer"
  >
    Volver al inicio
  </button>
</div>

<script>
  const loginButton = document.getElementById(
    "button"
  ) as HTMLButtonElement | null;
  if (loginButton) {
    loginButton.addEventListener("click", () => {
      window.location.href = "/landing";
    });
  }
</script>
